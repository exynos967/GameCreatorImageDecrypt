# GameCreatorImageDecrypt
GameCreator图像资源的解密脚本，目前用的暴力搜索。。。也许会有更好的办法

使用方法：
- 将待处理 PNG 放入 `input/` 目录（脚本同级，若不存在会自动创建）
- 运行：`python decrypt.py`
- 解密结果输出到 `output/` 目录（自动创建），命名为 `decrypted_<原文件名>`

如果帮到你了，不如点个Star~
